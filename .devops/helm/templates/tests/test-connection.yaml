apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "painel-de-bordo.fullname" . }}-test-connection"
  labels:
    {{- include "painel-de-bordo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "painel-de-bordo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
