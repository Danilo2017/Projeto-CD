apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "painel-de-bordo.fullname" . }}
  labels:
    {{- include "painel-de-bordo.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "painel-de-bordo.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        checksum/config: {{ .Files.Get ".env" | sha256sum }}
      labels:
        {{- include "painel-de-bordo.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "painel-de-bordo.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          volumeMounts:
          - name: envfile-volume
            mountPath: /var/www/html/.env
            subPath: .env
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
              protocol: TCP
          env:
            - name: DD_AGENT_HOST
              valueFrom:
                fieldRef:
                  fieldPath: status.hostIP
            - name: DD_SERVICE
              value: "{{ .Values.ddtrace.service }}"
            - name: DD_ENV
              value: "{{ .Values.ddtrace.env }}"
            - name: DD_VERSION
              value: "{{ .Values.ddtrace.version }}"
            - name: DD_LOGS_INJECTION
              value: "false"
            - name: DD_TRACE_CLI_ENABLED
              value: "false"
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      volumes:
      - name: envfile-volume
        configMap:
          name: envfile
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
